<div
    class="sari-mobile-menu"
    id="sariMobileMenu"
    aria-hidden="true"
>
    <nav class="sari-mobile-nav" aria-label="Mobile navigation">
        <a href="{{ route('home') }}#home">Home</a>
        <a href="{{ route('home') }}#discover">Discover</a>
        <a href="{{ route('home') }}#about">About</a>
        <a href="{{ route('home') }}#how-it-works">How It Works</a>
    </nav>

    <div class="sari-mobile-actions">
        <a href="{{ route('login') }}">Login</a>
        <a href="{{ route('home') }}#register">Register</a>
    </div>
</div>
