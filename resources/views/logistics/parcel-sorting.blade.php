@extends('layouts.logistics')
@section('title','Parcel Sorting')
@section('page-title','Parcel Sorting')
@section('content')
<div class="mx-auto max-w-[1600px]">@if(session('success'))<div class="mb-4 rounded-xl border border-emerald-200 bg-emerald-50 p-3 text-[9px] text-emerald-700">{{ session('success') }}</div>@endif<section class="rounded-[22px] border border-[#ebe4da] bg-white p-5 sm:p-6"><h2 class="text-[20px] font-bold">Sorting Queue</h2><p class="mt-2 text-[9px] text-[#81786c]">Received parcels must be sorted before final Rider delivery.</p></section><div class="mt-4 space-y-3">@forelse($parcels as $parcel)<article class="rounded-[18px] border border-[#ebe4da] bg-white p-4"><div class="flex flex-col gap-3 lg:flex-row lg:items-center lg:justify-between"><div><p class="text-[10px] font-bold">{{ $parcel->order?->order_number }}</p><p class="mt-1 text-[8px] text-[#918677]">{{ $parcel->order?->seller?->store_name ?: 'Seller' }} → {{ $parcel->order?->buyer_name }}</p><p class="mt-1 text-[7px] text-[#a0978c]">Received {{ $parcel->received_at?->format('M d, Y h:i A') ?: '—' }}</p></div>@if($parcel->status==='received')<form method="POST" action="{{ route('logistics.parcel-sorting.sort',$parcel) }}" class="flex flex-col gap-2 sm:flex-row">@csrf<input name="sorting_zone" required maxlength="80" placeholder="Zone / rack (e.g. A-01)" class="h-10 rounded-xl border px-3 text-[8px]"><input name="notes" maxlength="1000" placeholder="Optional note" class="h-10 rounded-xl border px-3 text-[8px]"><button class="rounded-xl bg-[#56816a] px-4 py-2 text-[8px] font-bold text-white">Mark Sorted</button></form>@else<div class="text-right"><span class="rounded-full border border-emerald-200 bg-emerald-50 px-3 py-1.5 text-[7px] font-bold text-emerald-700">SORTED · {{ $parcel->sorting_zone }}</span><p class="mt-1 text-[7px] text-[#918677]">{{ $parcel->sorted_at?->format('M d, h:i A') }}</p></div>@endif</div></article>@empty<div class="rounded-[18px] border border-dashed p-10 text-center text-[9px] text-[#918677]">No parcels in the sorting queue.</div>@endforelse</div></div>
@endsection
